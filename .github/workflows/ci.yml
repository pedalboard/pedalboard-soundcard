name: "Hardware CI"

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths:
      - "*.kicad_sch"
      - "*.kicad_pcb"
      - "*.kibot.yaml"
      - "site/**"
      - ".github/workflows/ci.yml"
  pull_request:
    paths:
      - "*.kicad_sch"
      - "*.kicad_pcb"

permissions:
  contents: write

jobs:
  site:
    uses: laenzlinger/hugo-kicad-site/.github/workflows/kicad-site.yml@main
    with:
      project_name: "pedalboard-soundcard"
      schematic: "pedalboard-soundcard.kicad_sch"
      pcb: "pedalboard-soundcard.kicad_pcb"
      kibot_config: "pedalboard-soundcard.kibot.yaml"
      site_base_url: "https://pedalboard.github.io/pedalboard-soundcard"
      container_image: "ghcr.io/inti-cmnb/kicad10_auto_full:dev"
